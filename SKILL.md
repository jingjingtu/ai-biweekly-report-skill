---
name: ai-biweekly-report
description: Use when users need to turn AI project plans, fortnightly progress, design-system work, workflow experiments, validation evidence, blockers, or next actions into a Chinese AI biweekly design report, fixed 1920x1080 dark technology-style HTML slides, or editable Figma-ready presentation.
---

# AI 双周会设计汇报

将计划、进展和证据组织成深色科技风双周会页面。核心不是罗列任务，而是回答四个问题：目标是否变化、本期推进了什么、有什么证据、下一周期验证什么。

## 输入契约与证据口径

开始前，将素材整理为下列最小输入；信息缺失不阻塞生成，但必须标为“待采集 / 待确认”，不能补造事实。

```text
period：汇报周期
 audience：management | project-team | mixed（默认 mixed）
 conclusion：本期一句话结论
 northStar：北极星目标 / 当前阶段
 progress[]：动作、产出、证据链接或截图、状态（DONE / VERIFY / NEXT）
 metrics[]：目标值、当前值、统计截止日期、数据来源、验证状态
 tradeoffs[]：问题、判断、取舍
 nextActions[]：下期动作、交付物、验收口径
```

- `management` 优先结论、风险、资源诉求和阶段门槛；`project-team` 保留方法、证据和执行细节。
- 无真实数据时只写“待采集基线 / 验证中”，并写清下一次采集或验证动作。


## 执行流程

1. 读取用户的计划、历史汇报、截图和相关 Skill。把信息分成：年度目标、本期动作、交付证据、问题判断、下期动作。
2. 区分目标值与实际值。年度 `80%` 等指标必须标记为 `YEAR-END TARGET`；没有真实数据时使用“待采集基线 / 验证中”，禁止编造完成率。
3. 先写一句本期结论，再组织页面。双周会默认结构：
   - 封面 / 本期主题
   - 北极星目标与能力路径
   - 年度目标指标带
   - 当前阶段与三项推进卡
   - 交付证据或真实案例
   - 问题、判断与取舍
   - 下个双周动作
   - 本期结论
4. 用户要求浓缩成 3 页时，不要把默认结构硬压缩。改用三页叙事：`本期判断`、`做了什么 / 体验节奏`、`验证入口 / 反馈闭环`。详细规则见 [内容与模块映射](references/content-mapping.md)。
5. 根据内容选模块。详细规则见 [内容与模块映射](references/content-mapping.md)。
6. 使用 [视觉与绘图规范](references/style-system.md)，从 [HTML 模板](assets/template.html) 复制并替换内容。每个 `.page` 必须固定为 `1920px × 1080px`，内容超出时拆成下一页，禁止纵向增长。每张指标卡、进展卡、证据卡和下一步卡都添加唯一的 `data-figma-block`，名称使用可读中文。
7. 在浏览器中逐页检查 `1920 × 1080` 画面；确认根节点尺寸准确，且无文字溢出、重叠、空占位和虚假数字。
8. 用户要求导入或生成 Figma 时，必须优先保证 Figma 原生可编辑结构。直接写 Figma 时，每个页面、章节、卡片、标签组、流程组都必须是 Auto Layout Frame；HTML 导入后也要按 `data-figma-block` 做模块化整理，禁止只留下分散的文字、矩形和线条。
9. 用户要求参考某个 Figma 画板、“多一些样式发挥”、“厚实视觉版”或指出“太平 / 太薄 / 字体粘连”时，先提炼参考画板的视觉语言，再按 [视觉与绘图规范](references/style-system.md) 中的“厚实视觉版”规则在空白区域生成独立新版，不覆盖已确认版本。视觉可以更有表现力，但结构仍必须可编辑、可验收。
10. Figma 生成或整理完成后，必须执行结构 QA，而不是只看截图。至少检查：无画面外元素、无 `height<=1.5px` 文本、无小于 `24px` 的叙事文字、微型状态标签是否满足例外规则、标签文字填满容器并居中、页头状态和页脚页码右边界一致。任何一项失败都先修复并复查，不能直接回复“完成”。
11. 用户明确需要高保真 HTML 导入时，再调用 `html-to-figma-fidelity`；导入完成后仍要检查并整理为 `页面 Root → 章节 Section → 内容模块/卡片 Auto Layout → 文本/图形/图片` 的层级。

## 写作规则

- 标题写“变化”而非“事项”：例如“从文件迁移，推进到可复用生成能力”。
- 每项进展使用 `动作 → 产出 → 证据 → 意义`，最多四行。
- 区分三种状态：`DONE 已完成`、`VERIFY 验证中`、`NEXT 下一步`。
- 不同语义层级不能做成同级卡片。例如“希望大家反馈的 4 个问题”和“团队收到反馈后的 3 个处理动作”不能平铺在同一个卡片网格里；前者是用户任务，后者应降级为流程条、说明带或下一步备注。
- AI 工作必须说明 AI 与设计师的边界：AI 负责结构生成和重复劳动，设计师负责规则、判断、精修和验收。
- 遇到问题必须给出取舍，不写“持续优化”这种空话。
- 下一步必须可验收，写清交付物或验证口径。
- 输入材料不足时先基于已知事实生成，并把缺失数据写成“待采集”，不要停在空白模板。

## 页面选型

- **计划首次发布**：突出北极星、目标指标、阶段路线图。
- **常规双周会**：突出本期结论、三项推进、证据、问题与下期动作。
- **阶段复盘**：突出计划对照、完成证据、偏差、方法沉淀与下一阶段门槛。
- **单项目深挖**：突出背景、矛盾、设计决策、前后对比、结果和可复用资产。

## 强制视觉规则

- 每页尺寸必须为 `1920 × 1080`，一页只表达一个核心结论；多项主题拆页，不制作汇报长页。
- 页面安全边距建议左右 `80px`、上下 `64px`；页内有效内容高度不得超过 `952px`。
- 所有可见文字字号不得小于 `24px`。如果信息放不下，优先拆页、精简文案或改成图表/标签组，禁止通过缩小字号硬塞内容。
- Figma 文字节点禁止出现 `height=1px`。单行文字节点高度必须等于 `fontSize + 8px`；多行文字必须使用 `lineHeight = fontSize * 1.6`，并在写入文字后重新计算节点高度。
- 整体布局必须遵循 `8px` 倍数原则：页面边距、模块宽高、卡片内边距、间距、圆角、坐标位置、图片容器尺寸优先使用 `8` 的倍数；描边允许 `1px`，其余非 8 倍数必须有明确视觉或素材比例原因。
- 空间留白不能过多。安全区内主要内容应形成稳定信息密度；当主体内容明显不足、大片区域无信息承载时，必须通过补充证据截图、流程关系、状态刻度、图注、对照说明或重新分栏来消化空间，禁止用空卡片、超大间距或无意义装饰撑版面。
- 内容卡片的内容安全间距默认上/下/左 `24px`；右侧间距不得小于 `24px`，可根据字数、列宽和视觉重心增加到 `32px` 或 `40px`，但不能让正文贴近边框。
- 三等分卡片、三阶段卡、三张下一步卡之间的水平间距固定优先使用 `32px`。如果版式必须使用更大空隙，空隙必须承载坐标线、连接线、编号、状态刻度或其他信息型装饰，禁止出现无意义大空白。
- 近黑底，不使用大面积彩色渐变；绿色是主线，不是全页填色。
- 紫色只表示工程化/生成能力，青色表示验证/连接，橙色表示风险/下一阶段。
- 卡片圆角 `8px`，大容器圆角使用 `16px`；细描边优先于阴影。
- 使用点阵、坐标线、终端眉题、编号和箭头构成技术感，不使用装饰性光球。
- 大标题白色，结论中的关键词绿色；正文保持克制灰度。
- 每个视觉模块必须承载信息，不为“科技感”增加无意义图形。
- Figma 原生输出必须是可编辑的布局结构，不是散元素堆叠。层级固定为 `页面 Root → 章节 Section → 内容模块/卡片 Auto Layout → 文本/图形/图片`；章节背景必须位于卡片下方，卡片背景和对应文字必须在同一个可选择内容块中。
- 同一语义模块必须放在同一个 Auto Layout Frame 内：例如一张指标卡的背景、编号、标题、说明、状态标签属于一个卡片 Frame；一组三个标签属于一个标签组 Frame；一个流程步骤的编号、标题、连接线和说明属于一个流程组 Frame。禁止让这些内容平铺在页面 Root 下。
- Auto Layout 默认规则：页面 Root 用垂直布局承载页头、主体和结论；主体根据版式用水平或垂直布局；卡片用垂直布局；标签组和指标带用水平/换行布局。只有跨模块连接线、点阵背景、截图遮罩等装饰/覆盖物可以绝对定位，并必须单独命名为 `Overlay / ...` 或 `Connector / ...`。
- 模块 Frame 必须设置清晰命名和固定/填充尺寸：如 `Card / 本期推进 01`、`Module / 交付证据`、`TagGroup / 验收项`。重复卡片之间优先使用 Auto Layout 的 `itemSpacing=32`，不要用手动 x/y 摆放制造间距。
- 标签、胶囊按钮和流程步骤里的文字必须真正居中：Figma 中容器使用水平 Auto Layout，水平/垂直对齐均为 `CENTER`；内部文字节点固定为容器宽高，`textAlignHorizontal=CENTER` 且 `textAlignVertical=CENTER`。不能只让文字 Hug 内容后放在容器里，否则选中或导出时容易出现视觉贴边。
- 页脚页码和页头右侧状态必须按版心右边界对齐。多页汇报的 `01 / 03`、`02 / 03`、`03 / 03` 等页码使用固定宽度文字框并右对齐，右边界与页面安全区右侧一致。
- Figma QA 是交付前硬门槛：如果标签、页码、文字高度、字号、画面边界或 Auto Layout 结构不合格，必须先修正当前画板并重新验证；不要把 Skill 规则当成说明文档停留在文本层。

## 验收清单

- 是否一屏就能看懂本期结论与当前阶段？
- 计划指标与实际进展是否明确分开？
- 每项进展是否有可指向的交付物或验证证据？
- 是否写清问题、取舍和下一步，而不是只有完成清单？
- 是否保持黑/绿主线，并用紫、青、橙承担固定语义？
- 页面是否无 `XX%`、`xx月xx日`、Lorem 或未解释占位符？
- 若缺数据，是否明确显示“待采集基线”而非伪造数字？
- 每个页面根节点是否严格为 `1920 × 1080`，且没有依赖滚动才能看到的内容？
- 页面中所有叙事文字字号是否都不小于 `24px`，微型状态标签是否符合 `20–24px` 例外规则？
- 页面边距、模块尺寸、卡片间距、内边距、圆角和主要坐标是否遵循 `8px` 倍数原则？
- 主体安全区是否没有无信息承载的大块留白；若内容偏少，是否已通过证据、图注、流程关系或重新分栏提升信息密度？
- Figma 中每个主要模块是否可以一键选中整块移动，且不是散落在 Root 下的一堆文字、矩形和线条？
- 卡片、标签组、流程组是否使用 Auto Layout Frame，背景和文字是否在同一个模块容器内？
- 标签、胶囊按钮和流程步骤中的文字是否填满容器并水平/垂直居中？
- 页脚页码与页头右侧状态是否按版心右边界对齐，且多页位置一致？
- 是否已经用 Figma 节点检查验证上述对齐、字号、高度和越界问题，而不是只肉眼看截图？
- Figma 文本节点是否没有 `height=1px`，单行高度是否为字号 + 8px，多行行高是否为 1.6 倍？
- 内容卡片上/下/左是否为 24px，右侧是否根据画面自动调整且不小于 24px？
- 三卡布局是否优先 32px 间距；若存在大空白，是否被信息型装饰或连接关系使用？

## 与其他 Skill 配合

- 月度总结、个人价值提炼：先使用 `monthly-report`，再用本 Skill 改成双周节奏。
- HTML 高保真导入 Figma：使用 `html-to-figma-fidelity`。
- Figma 中文文本写入与验收：使用 `chinese-figma-text-guard` 和 `figma-report-layout-qa`。
