---
name: ai-biweekly-report
description: Create Chinese AI design-team biweekly reports from plans, progress, evidence, risks, decisions, and next actions. Use for report outlines, fixed 1920x1080 HTML slides, 1920px long-scroll reports, or editable Figma-ready plans; do not trigger for generic status prose or unrelated presentations.
---

# AI 双周会设计汇报

把 AI 项目的目标、变化、证据、取舍和下一步组织成可讲述、可验证的中文双周会汇报。默认生成固定 1920 × 1080 的深色科技风 HTML；用户指定长图、长卷或标准样例时生成 1920px 宽连续长卷。用户只要内容时停在大纲，明确要求 Figma 时再进入 Figma 阶段。

## 输出模式

先确定输出模式，避免让某一种工具的缺失阻塞其他交付：

- `content`：只输出内容大纲、页级结论和讲述顺序。
- `html`：默认模式；输出可直接浏览的 1920 × 1080 HTML。
- `figma`：用户明确要求时，在内容或 HTML 已完成的基础上生成/整理 Figma。
- `all`：用户明确要求同时交付内容、HTML 和 Figma。

若用户没有明确指定，视觉汇报请求默认使用 `html`；“先帮我梳理汇报逻辑”等请求使用 `content`。

输出媒介之外，还要确定画布格式：

- `slides`：默认；独立 1920 × 1080 页面。
- `long-scroll`：用户指定长图、长卷、网页式汇报，或明确要求匹配本 Skill 的标准样例时使用；宽度 1920px，高度随章节增长。

## 输入与事实纪律

按 [输入契约](references/input-contract.md) 整理素材。优先确认汇报目的、听众、需要推动的决策、当前结论、证据和下一步责任人。

- 信息缺失不阻塞内容或 HTML 生成；使用“待采集 / 待确认”，并写明补充动作。
- 目标值、实际值和推测必须分开。没有来源、截止日期或统计口径的数字不能包装成已验证成果。
- 计划中的 `80%` 等指标标记为 `TARGET`；实际结果必须能追溯到材料、文件或用户确认。
- 状态统一使用 `DONE`、`VERIFY`、`BLOCKED`、`NEXT`、`BASELINE`。
- AI 负责结构化、重复生成和检查；设计师负责业务判断、取舍、精修与验收。

## 执行流水线

1. **规范化输入**：整理周期、听众、汇报目的、决策诉求、本期变化、证据、风险和下一步。不要补造事实。
2. **形成结论**：先写一句本期判断，再组织支撑证据。标题描述“发生了什么变化”，不写任务名堆叠。
3. **选择内容逻辑**：任务涉及 B 端组件、页面模式、平台适配、Skill 或生成能力验证时，读取 [B 端历史汇报内容标准](references/b-end-content-standard.md)，默认使用 `narrativeMode: b-end-validation`。它决定结论与章节顺序。
4. **匹配视觉标准**：用户给出视觉样例或要求“按标准样例”时，读取 [视觉样例审查](references/sample-standard.md) 和 [原始视觉参考索引](references/visual-reference-index.md)，逐张审查与当前内容最接近的原图，记录 `format`、`sampleArchetype`、复用形式、装饰语法和不可照搬项。不能只凭对样例的文字摘要生成，也不能让视觉样例改写 B 端内容逻辑。
5. **规划页面/章节**：依据 [内容与模块映射](references/content-mapping.md) 选择页数或长卷章节、页面结论和 Layout Profile。用户提供多种版式样例或长卷包含 4 个以上主要章节时，先按 [视觉规范的版式多样性门禁](references/style-system.md#版式多样性门禁) 形成 `sectionLayoutPlan`，再开始渲染或写入 Figma；不能先复制统一骨架再换标题和颜色。年度指标、截图或路线图只在材料真实存在且有助于叙事时使用。
6. **生成内容**：普通进展使用“动作 → 产出 → 证据 → 意义”；B 端验证使用“问题 → 根因 → 规则 → 复验 → 沉淀”。下一步写清负责人、期限、交付物或验收口径。
7. **生成 HTML**：读取 [HTML 输出规范](references/html-output.md)，优先使用 `scripts/render-report.mjs` 从结构化 JSON 生成页面，再运行 `scripts/validate-report.mjs`。
8. **可选 Figma**：仅当输出模式包含 Figma 时读取 [Figma 输出规范](references/figma-output.md)。Figma 不可用只阻塞 Figma 阶段，不得否定已经完成的内容或 HTML。
9. **验证**：运行静态校验，并在浏览器可用时检查溢出、重叠、留白、对齐和截图观感。B 端任务执行内容闭环验收；使用视觉样例时再执行视觉一致性验收。完整要求见 [测试与验收](references/testing.md)。

用户要求检查或审查 Skill、样例或交付物时，必须读取 [五角色审查清单](references/review-checklist.md)，分别从架构师、设计师、产品经理、研发和测试角度报告，再合并成一份按优先级排序的修改清单。

## 模式化失败边界

- **内容阶段**：缺少数据时继续生成，标记待确认。只有缺少决定报告方向的核心选择且无法合理推断时才询问用户。
- **HTML 阶段**：渲染失败时先根据错误修正输入或模板；连续得到同一错误且没有新的安全替代方案时停止该阶段。浏览器不可用时仍交付通过静态校验的 HTML，并标记“视觉检查待完成”。
- **Figma 阶段**：无编辑权限、工具不可用或需要用户授权时停止 Figma 阶段，保留并交付已完成的内容和 HTML。
- **外部操作**：发布、分享、覆盖已确认版本或修改权限前，遵守当前环境的授权要求。Skill 不扩大用户授权范围。

停止某一阶段后，说明：已完成内容、具体阻塞、用户只需完成的动作，以及可从哪一步继续。

## 内容与视觉原则

- 一页只讲一个核心判断；页数由信息量和汇报时长决定，不强制套固定八页结构。
- 三页浓缩版使用“本期判断 → 做了什么/证据 → 验证入口/反馈闭环”。
- B 端验证汇报优先使用“本期变化 → 问题证据 → 根因判断 → 规则化解决 → 复验 → 能力沉淀 → 下一步”，不以完成清单代替结论。
- 观众要做的事与团队内部处理动作不能做成同级卡片。
- 主色表达内容类别：绿色为主线，紫色为工程化/生成能力，青色为验证/连接，橙色为风险/下一阶段。完成状态使用独立状态标签，不依赖颜色。
- `slides` 页面固定 1920 × 1080；`long-scroll` 固定 1920px 宽并按章节自然增高。所有可见文字（包括眉题、标签、图注、状态和页脚）均不得小于 24px；内容放不下时拆页、拆章节或换 Layout Profile。
- 用户要求长图时，交付物必须是一整块连续的根画布/根 Frame；小节是根画布内连续排列的可编辑章节，每节保留 48px 以上大标题、正文和证据区，不能重新拼成彼此分离的页面。
- 视觉丰富度来自内容驱动的构图变化、证据载体和克制装饰，不来自重复卡片、随机光球或无意义图形。每个长卷章节都要按 [视觉规范](references/style-system.md#克制的装饰与丰富度) 选择装饰语法并与相邻章节形成明显差异。
- 视觉细节、布局和 Figma 节点规则分别见 [视觉规范](references/style-system.md) 与 [Figma 输出规范](references/figma-output.md)。

## 自包含与可选能力

本 Skill 的内容和 HTML 输出不得依赖其他自定义 Skill。若当前环境恰好提供更专业的汇报、HTML→Figma 或文字 QA 能力，可以在用户任务范围内选用；能力不存在时使用本 Skill 的模板、脚本和验收规则完成可交付部分，不得虚构工具或把可选能力写成硬前置。

## 最低交付门槛

- 一屏能看懂本期结论、当前阶段和下一步。
- 计划值与实际结果清晰分离，证据可追溯。
- 无 `XX%`、`xx月xx日`、Lorem、未替换模板变量或不完整页码。
- HTML 中每个主要内容块拥有唯一、可读的 `data-figma-block`。
- 页面尺寸、字号、页码、颜色语义、溢出和重复标识通过校验。
- B 端任务已记录 `narrativeMode` 和历史内容样例依据；使用视觉样例时已记录 `format`、`sampleArchetype`，且未复用无来源的案例数字或品牌资产。
- 长卷为单一连续根画布，章节大标题清楚；所有可见文字不小于 24px；版式多样性和装饰丰富度均通过门禁。
- Figma 输出存在时，主要模块为可编辑 Auto Layout 结构，而非散落元素。
